import { expect, test } from 'vitest';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { useQueryFinnHub, useFinnHubSymbols, useFinnHubCandles } from './index.ts';

test('useQueryFinnHub', () => {
    expect(useQueryFinnHub).toBe();
});
